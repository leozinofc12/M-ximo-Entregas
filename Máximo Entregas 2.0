import React, { useState } from 'react';
import { View, Text, Button, StyleSheet, Alert } from 'react-native';

export default function App() {
  const [pedidoStatus, setPedidoStatus] = useState("Aguardando pedido");

  const confirmarPedido = () => {
    setPedidoStatus("Pedido confirmado!");
    Alert.alert("Sucesso", "Pedido confirmado com sucesso!");
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Máximo Entregas</Text>
      <Text style={styles.status}>{pedidoStatus}</Text>
      <Button title="Confirmar Pedido" color="#28A745" onPress={confirmarPedido} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0B1F48',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20
  },
  title: {
    fontSize: 28,
    color: '#FFFFFF',
    marginBottom: 20
  },
  status: {
    fontSize: 20,
    color: '#FFD700',
    marginBottom: 20
  }
});
